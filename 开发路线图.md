# xian 开发路线图

基于白皮书 v5.0 与现有代码库的分步实施指南。

---

## 阶段 0：理解现有代码（已就绪）

### 现有模块

```
src/
├── config/         # ✅ 配置系统（AiConfig 已定义）
├── narrative/     # 叙事层（基于 Git commit，需改造）
├── engine/        # ✅ 游戏引擎（已集成挂机循环）
├── storage/       # ✅ 存档系统（SQLite 可复用）
├── ui/            # ✅ TUI 界面（Ink + React）
├── osync/         # 🗑️ 将移除（Git 映射机制）
└── shared/        # ✅ 工具函数
```

---

## 阶段 1：AI 基础设施 ✅ 完成

**目标**：搭建 AI 内容生成能力

### 1.1 定义生成内容类型 ✅

- `src/ai/types.ts` - 功法/怪物/道具/事件/NPC/天劫/世界事件类型

### 1.2 创建 Prompt 模板 ✅

- `src/ai/prompts.ts` - 8 种生成器 Prompt

### 1.3 实现生成器核心 ✅

- `src/ai/generator.ts` - 支持 OpenAI/DeepSeek/Anthropic/Ollama

### 1.4 接入配置 ✅

- `src/config/defaults.ts` - ai.enabled = true

---

## 阶段 2：游戏核心循环 ✅ 完成

**目标**：从"写代码驱动"改为"挂机自动循环"

### 2.1 行为树 ✅

- `src/engine/behavior-tree.ts` - 战斗/修炼/探索/休息逻辑

### 2.2 挂机循环 ✅

- `src/engine/idle-loop.ts` - 每 5 秒自动执行行为树

### 2.3 集成到 GameEngine ✅

- `src/engine/index.ts` - 添加 startIdleLoop()/stopIdleLoop()

---

## 阶段 3：大世界地图系统（待开发）

**目标**：实现程序员成长路径的区域

### 3.1 定义区域数据

- `src/world/zones.ts` - 16 个区域定义

### 3.2 区域进入逻辑

- 境界达标解锁新界面

### 3.3 传送系统

---

## 阶段 4：AI 生成集成（待开发）

**目标**：内容动态化

- 区域首次进入 → AI 生成背景/怪物/NPC
- 境界提升 → AI 生成功法
- 战斗掉落 → AI 生成道具
- 随机事件 → AI 生成事件
- 境界突破 → AI 生成天劫

---

## 阶段 5：UI 改造（待开发）

**目标**：适应新玩法

- 修改主界面 HUD
- 新增命令：战斗/修炼/探索/地图
- 添加区域地图显示

---

## 阶段 6：整合测试

### 已完成

- [x] AI 生成器能生成功法/怪物/事件
- [x] 挂机循环正常运转
- [x] 基础数值系统工作

### 待完成

- [ ] 大世界地图可探索
- [ ] 区域解锁机制正常
- [ ] AI 内容正确填充
- [ ] 完整游戏循环
- [ ] 天劫系统
- [ ] Boss Key 可用

---

## 关键文件清单

### 已创建（阶段1-2）

```
src/ai/
├── types.ts        ✅
├── prompts.ts     ✅
├── generator.ts  ✅
└── index.ts      ✅

src/engine/
├── behavior-tree.ts  ✅
├── idle-loop.ts    ✅
└── index.ts       ✅ (已更新)
```

### 需要新建

- `src/world/zones.ts`

---

## 下一步

1. **启动游戏**：运行 `npm run start`
2. **手动启动挂机循环**：在游戏启动后调用 `engine.startIdleLoop()`
3. **或自动启动**：修改入口逻辑，当 AI 配置好时自动启用挂机模式

---

## 使用方式

```typescript
import { GameEngine } from './engine/index.js'

const engine = new GameEngine()
await engine.load() // 或 createPlayer()

// 启动挂机循环（离线放置模式）
engine.startIdleLoop()

// 或者停止挂机循环
engine.stopIdleLoop()
```

---

## 开始建议

1. 先测试阶段1的 AI 生成功能
2. 然后测试阶段2的挂机循环
3. 确认两个功能都正常后，继续阶段3
